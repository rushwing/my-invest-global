---
req_id: REQ-034
title: "Phase 10 — 账户级 Holdings 聚合展示"
status: pr_draft
owner: human-001
priority: P2
phase: PHASE-010
scope: engine/portfolio.py, app/dashboard.py
tc_policy: required
tc_exempt_reason: ""
depends_on: [REQ-032]
test_case_ref: [TC-034-01]
acceptance: "load_holdings_raw() 返回 8 行原始数据（无聚合）；dashboard Holdings 标签含三子 tab（合并/华西/西南）；华西子 tab 显示 3 只股票，西南子 tab 显示 5 只"
review_round: 0
pending_bugs: []
blocked_reason: ""
blocked_from_status: ""
blocked_from_owner: ""
pr_number: ""
---

# Goal

Dashboard Holdings 标签增加账户分组视图，让用户能分别查看华西证券（3只）和西南证券（5只）的持仓明细，以及合并后的总览（7只聚合）。

# In Scope

## `engine/portfolio.py` — load_holdings_raw

```python
def load_holdings_raw(db_path: str = "") -> list[HoldingRow]:
    """Load holdings without deduplication. Returns all 8 raw rows."""
    import yaml
    from engine.schemas import HoldingRow
    if not _HOLDINGS_YAML.exists():
        return []
    data = yaml.safe_load(_HOLDINGS_YAML.read_text())
    return [HoldingRow(**row) for row in (data.get("holdings") or [])]
```

## `app/dashboard.py` — Holdings 标签三子 tab

```python
tab_merged, tab_huaxi, tab_xinan = st.tabs(["合并视图", "华西证券", "西南证券"])

with tab_merged:
    # 现有聚合持仓表（load_holdings() 返回 7 只）+ portfolio balance 卡片

with tab_huaxi:
    raw = load_holdings_raw(DB_PATH)
    huaxi = [h for h in raw if h.account == "华西证券"]
    # 显示持仓表 + 账户总市值

with tab_xinan:
    raw = load_holdings_raw(DB_PATH)
    xinan = [h for h in raw if h.account == "西南证券"]
    # 显示持仓表 + 账户总市值
```

# Out of Scope

- 多账户动态配置（硬编码两个账户名足够）
- 账户级别的 portfolio balance 计算（仅显示市值合计）

# Acceptance Criteria

- `TC-034-01`：`load_holdings_raw()` mock YAML 返回 8 行，account 字段各自正确；按 account 过滤后华西 3 只、西南 5 只

# Implementation Notes

- 依赖 REQ-032 的 `account` 字段；在 REQ-032 merge 后实施
- `load_holdings_raw` 不调用 `_aggregate_holdings`，直接返回 YAML 原始行
