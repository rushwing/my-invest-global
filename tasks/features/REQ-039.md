---
req_id: REQ-039
title: "Phase 11 — 仪表盘筹码面板（Tab 3 集成 / 独立页）"
status: req_review
owner: evaluator-001
priority: P2
phase: PHASE-011
scope: app/pages/chip_panel.py, app/dashboard.py
tc_policy: required
tc_exempt_reason: ""
depends_on: [REQ-037, REQ-038]
test_case_ref: []
acceptance: "render_chip_panel('688143', 131.60, mock_analysis) 不抛异常；水平直方图包含当前价格标注线；ChipSummaryCard 显示 above_90_band=True 时红色警告样式；uv run streamlit run app/dashboard.py 启动后筹码面板可正常渲染"
review_round: 0
pending_bugs: []
blocked_reason: ""
blocked_from_status: ""
blocked_from_owner: ""
pr_number: ""
---

# Goal

在仪表盘中为持仓标的提供筹码分布可视化面板，包含水平直方图和关键指标卡片。
由于 Tab 3（REQ-018）尚未实现，本 REQ 先作为独立 Streamlit 页发布（`app/pages/chip_panel.py`），
待 Tab 3 实现后（REQ-018）无缝集成。

# In Scope

## `app/pages/chip_panel.py`

### 页面入口

```python
def chip_panel_page() -> None:
    """独立 Streamlit 页，可通过 st.navigation 或直接 streamlit run 访问。"""
    st.title("筹码分析")
    code = st.selectbox("选择标的", load_holdings_codes())
    current_price = st.number_input("当前价格", min_value=0.01, value=0.0, format="%.2f")
    if st.button("分析"):
        _render_chip_panel(code, current_price)
```

### 核心渲染函数

```python
def render_chip_panel(
    code: str,
    current_price: float,
    analysis: ChipAnalysis,    # 由调用方传入，面板本身不调用 API
) -> None:
    """纯渲染函数，便于 Tab 3 集成时直接调用。"""
```

### 组件 1：水平筹码直方图

使用 `plotly.graph_objects.Bar` + `orientation='h'`：

- X 轴：`chip_ratio`（百分比，右→）
- Y 轴：价格档位中点（`(price_lower + price_upper) / 2`）
- 颜色规则：
  - 价格 < `current_price`：`#00C47A`（获利区，绿色）
  - 价格 ≥ `current_price`：`#E84040`（套牢区，红色）
- 标注线（`plotly shapes`）：
  - 实线橙色：`current_price`，标签 "现价"
  - 虚线白色：`avg_cost`，标签 "均成本"
  - 虚线灰色：`range_90_lower` / `range_90_upper`，标签 "90%区间"
- 布局：深色背景（`bg=#0E1117`），高度 400px，Y 轴标签右对齐

### 组件 2：ChipSummaryCard

4 列 `st.metric` 展示：

| 列 | 指标 | 样式规则 |
|---|---|---|
| 收盘获利 | `f"{analysis.profitable_pct:.1%}"` | ≥ 95% 显示 ⚠ delta（获利盘压力大） |
| 平均成本 | `f"¥{analysis.avg_cost:.2f}"` | 偏离率 `cost_deviation_pct` 作 delta |
| 筹码集中度 | `f"{analysis.concentration:.1f}%"` | `chip_lock_level` 枚举对应中文标签 |
| 信号状态 | 见下方 | |

**信号状态卡片：**

```python
if analysis.above_90_band:
    st.error(f"⚠ 突破90%筹码上界（{analysis.range_90_upper:.2f}）— 获利盘兑现压力")
elif analysis.below_90_band:
    st.warning(f"价格低于90%筹码下界（{analysis.range_90_lower:.2f}）— 套牢密集区")
else:
    st.info(f"价格在90%筹码区间内（{analysis.range_90_lower:.2f}–{analysis.range_90_upper:.2f}）")
```

### 组件 3：输入来源切换

```python
source = st.radio("数据来源", ["API（akshare）", "截图解析（OCR）"], horizontal=True)
if source == "截图解析（OCR）":
    uploaded = st.file_uploader("上传同花顺筹码截图", type=["png", "jpg", "jpeg"])
    if uploaded:
        # 调用 parse_chip_screenshot，显示置信度提示
```

## `app/dashboard.py` — 集成占位

当 Tab 3（REQ-018）实现后，在 `tab_stock_depth` 内加入：

```python
with st.expander("筹码分析", expanded=False):
    render_chip_panel(selected_code, current_price, analysis)
```

本 REQ 暂不修改 `dashboard.py`，仅确保 `render_chip_panel` 接口稳定。

## Out of Scope

- Tab 3 完整实现（REQ-018 范围）
- 多标的筹码对比面板
- 筹码历史演变动画
- 移动端适配

# Acceptance Criteria

- `TC-039-01`：`render_chip_panel('688143', 131.60, mock_analysis_above_90)` 不抛异常；`st.error` 被调用（`above_90_band=True`）
- `TC-039-02`：水平直方图中 `current_price` 以下的 bars 颜色为 `#00C47A`，以上为 `#E84040`；标注线包含 "现价" 和 "均成本"
- `TC-039-03`：`ChipSummaryCard` 中 `profitable_pct=0.9973` 显示 `"99.7%"` 并附 delta 警告
- `TC-039-04`：`chip_panel_page()` 在 mock holdings 数据下可完整运行 `streamlit run` 无报错（手动验证，不做自动化 UI 测试）

# Implementation Notes

- 设计 token 与项目设计系统一致：`bg=#0E1117`，`surface=#1C1C2E`，`bull=#00C47A`，`bear=#E84040`，`neutral=#F5A623`
- `render_chip_panel` 为纯渲染函数（接受 `ChipAnalysis` 参数），使 TC-039-01..03 可在无 Streamlit server 环境下 mock `st.*` 调用后测试
- 截图上传组件（组件3）在 TC 中 mock `st.file_uploader` 返回 None，不测试真实 OCR 调用
- 参考 [Image #3] 中的行内迷你资金流条设计：筹码直方图可在 Tab 4 ScarcityMatrix 的股票 chip 中以 5 格迷你版集成（Phase 12 扩展点，此处不实现）
