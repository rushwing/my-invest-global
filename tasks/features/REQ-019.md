---
req_id: REQ-019
title: Tab 4 信号仪表盘
phase: phase-7
scope: fullstack
priority: P2
status: req_impl_review
owner: evaluator-001
tc_policy: required
branch: feat/PHASE-007-dashboard
review_round: 2
depends_on: [REQ-001, REQ-013, REQ-020]
test_case_ref: [TC-019-01, TC-019-02, TC-019-03, TC-019-04, TC-019-05, TC-019-06, TC-019-07, TC-019-08, TC-019-09, TC-019-10, TC-019-11, TC-019-12, TC-019-13]
acceptance: "macro_state.json state=green → 弹性仪表盘目标显示 38%；state=red → 显示 20%；ScarcityMatrix score=72 chip 颜色=#00C47A；mini strip 5 格颜色与 main_net_inflow 正负一致"
pending_bugs: []
blocked_reason: ""
blocked_from_status: ""
blocked_from_owner: ""
pr_number: ""
---

## Summary

实现 Tab 4 "信号仪表盘"：宏观周期面板（三色交通灯 + Big-4 云 CapEx 表）、板块紧缺度矩阵、仓位再平衡仪表盘（含触发器状态表）和历史再平衡时间线。

## Design Reference

`docs/design/dashboard/design_handoff_dashboard/tab4-signals.jsx`：
- `MacroCyclePanel`：3 态交通灯（发光效果）+ CapEx 表
- `ScarcityMatrix`：Tier 1–4 四列 chip 网格
- `Gauge`：半圆形 SVG 仪表盘（设计用 SVG）→ 实现用 `streamlit.components.v1.html`
- `RebalanceTriggers`：7 行触发器状态表
- `HistoryTimeline`：st.dataframe + 可选 Plotly scatter

## Acceptance Criteria

- `load_macro_state()` 返回 `{"state": "green"}` → 弹性仓位仪表盘目标显示 38%；`{"state": "red"}` → 显示 20%
- Gauge ±5% 区域颜色变化（容忍带）；±10% 触发器触发（两个不同阈值，不混淆）
- ScarcityMatrix chip 颜色：composite_score ≥ 70 → bull (#00C47A)；50–70 → neutral (#F5A623)；< 50 → bear (#E84040)
- ◉ 符号 = 当前持有股票；⚠ 符号 = warn=True 的股票
- RebalanceTriggers 表：status="triggered" 行显示红色背景
- 手动宏观覆盖按钮调用 `set_macro_state_override(MacroState(selected))`（REQ-001）后执行 `st.cache_data.clear(); st.rerun()`

## Components

### MacroCyclePanel

```
宏观 CapEx 周期状态
┌─────────────────────────────────────┐
│   ○ 收缩    ○ 平稳   ● 增长         │  ← 交通灯（激活态绿色 + glow）
├─────────────────────────────────────┤
│ 云厂商   FY2025A  FY2026E  YoY      │
│ MSFT     $80.3B   $95.0B  +18.3%   │
│ AMZN     $83.0B   $98.0B  +18.1%   │
│ GOOG     $52.1B   $62.0B  +19.0%   │
│ META     $40.0B   $49.0B  +22.5%   │
├─────────────────────────────────────┤
│ 综合判断: 大模型训练需求驱动 CapEx 超预期增长  │
│ [手动覆盖状态] 按钮                   │
└─────────────────────────────────────┘
```

交通灯 glow 效果（`streamlit.components.v1.html`）：
```css
.light.active { box-shadow: 0 0 16px 4px var(--color); }
```

CapEx 列名：fy2025（FY2025A），fy2026_est（FY2026E）；YoY = (fy2026_est - fy2025) / fy2025

### ScarcityMatrix

4 列 `st.columns(4)`，每列一个 Tier：

```
Tier-1 (橙色)    Tier-2 (黄色)    Tier-3 (灰色)    Tier-4 (蓝色)
┌───────────┐   ┌───────────┐   ...
│ ◉中际旭创  │   │  华为海思  │
│ 300308    │   │  (未上市)  │
│ score: 84 │   │ score: 78 │
└───────────┘
```

chip 颜色由 `composite_score` 决定：
```python
def chip_color(score: int) -> str:
    if score >= 70: return "#00C47A"
    if score >= 50: return "#F5A623"
    return "#E84040"
```

◉ = `owned=True`（当前持有）；⚠ = `warn=True`

### RebalanceTriggers + Gauge

仪表盘（半圆 SVG via `streamlit.components.v1.html`）：
- 白马：当前比例 vs 目标（宏观状态决定目标，Green→62%/Yellow→67%/Red→80%）
- 弹性：当前比例 vs 目标（Green→38%/Yellow→33%/Red→20%）
- ±5%：仪表盘颜色警告带（neutral 颜色）
- ±10%：触发器触发条件（rebalance fires）

```python
ELASTIC_TARGETS = {"green": 38, "yellow": 33, "red": 20}
target = ELASTIC_TARGETS[macro_state.lower()]   # macro_state 来自 load_macro_state()["state"]，已为小写
current = elastic_pct * 100
deviation = current - target

# Gauge 颜色
if abs(deviation) > 10: gauge_color = "#E84040"  # 触发
elif abs(deviation) > 5: gauge_color = "#F5A623"  # 警告
else:                     gauge_color = "#00C47A"  # 正常
```

7 触发器状态表（来自 project-scope.md § Dynamic Rebalance Triggers）：

| 触发器 | 方向 | 幅度 | 状态 |
|---|---|---|---|
| 云 CapEx 指引上调（季度） | 弹性股 ↑ | +7%，上限 40% | 正常/观察/触发 |
| 云 CapEx 指引下调 >10% | 弹性股 ↓ | -13%，下限 20% | ... |
| 板块整体回撤 >15% | 白马股 ↑ | +7%，上限 75% | ... |
| 板块突破前高（放量） | 弹性股 ↑ | +5%，上限 38% | ... |
| 单只弹性股亏损 >20% | 止损提示 | 建议减仓/退出 | ... |
| 单只持仓达目标价 80% | 止盈提示 | 建议减仓 30–50% | ... |
| 单只弹性股超 10% | 调仓提示 | 建议减至 <8% | ... |

status 颜色：`"normal"`=textMuted；`"watch"`=neutral；`"triggered"`=bear + 红色背景行

### 手动宏观覆盖调用契约

```python
from engine.macro_gate import MacroState, set_macro_state_override

# UI：st.selectbox 返回字符串 "green" / "yellow" / "red"
selected = st.selectbox("覆盖宏观状态", ["green", "yellow", "red"])
if st.button("应用覆盖"):
    set_macro_state_override(MacroState(selected))  # 写入 data/cache/macro_state.json
    st.cache_data.clear()                            # 清除 load_macro_state() 缓存
    st.rerun()
```

`set_macro_state_override()` 接受 `MacroState` enum（不接受裸字符串）。
`MacroState("green")` = `MacroState.GREEN`，因为 `MacroState(StrEnum)` 的值为小写字符串。
调用后 `data/cache/macro_state.json` 写入 `{"state": "green"}`（`set_macro_state_override` 当前实现仅写 state 字段，不写 override 标志）。

### HistoryTimeline

```python
st.dataframe(
    history_df.sort_values("date", ascending=False),
    column_config={
        "date":   st.column_config.DateColumn("日期"),
        "action": st.column_config.TextColumn("操作"),
        "reason": st.column_config.TextColumn("原因"),
    },
    use_container_width=True,
)
```

可选：Plotly scatter 时间线（x=date，y=0，text=action，hover=reason）

## MacroCyclePanel 顶部双图 Header（Scope Extension）

MacroCyclePanel 交通灯上方增加双图并排（`st.columns(2)`，高度 200px，深色背景）：

**左图 — 近 20 日持仓主力净流入汇总（`go.Bar`）**
```python
fund_flow_df = load_fund_flow_5d(codes=holding_codes, days=20)
daily = fund_flow_df.groupby("trade_date")["main_net_inflow"].sum().reset_index()
colors = ["#00C47A" if v >= 0 else "#E84040" for v in daily["main_net_inflow"]]
fig_left = go.Figure(go.Bar(x=daily["trade_date"], y=daily["main_net_inflow"],
                             marker_color=colors))
```

**右图 — 近 20 日涨跌家数比折线（`go.Scatter`）**
```python
breadth_df = load_market_breadth(days=20)   # [date, up_ratio]
fig_right = go.Figure(go.Scatter(x=breadth_df["date"], y=breadth_df["up_ratio"],
                                  line=dict(color="#4F8EF7", width=1.5)))
fig_right.add_hline(y=0.5, line_dash="dot", line_color="#888")
```

两图均用 `plot_bgcolor="#0E1117"`, `paper_bgcolor="#0E1117"`, `height=200`。

## ScarcityMatrix 行内增强（Scope Extension）

在每个 chip 卡片上增加两处内容：

### Tier badge + inline rationale（chip 内）
```html
<div class="chip-header">
  <span class="tier-badge tier-{tier}">Tier {tier}</span>
  <span class="rationale">
    {score:.0f}分 · {signal_text}
  </span>
</div>
```
- `tier` 来自 `stock_meta.rank`
- `signal_text`：score ≥ 70 → "强势区间"；50-70 → "震荡整理"；< 50 → "弱势区间"
- Tier 徽章颜色：1=#FF7A1A，2=#F5A623，3=#9AA0AC，4=#4F8EF7

### 5 日资金流 mini strip（chip 底部）
```html
<div class="flow-strip">
  <!-- 5 个格子，左旧右新，颜色=主力净流入正负 -->
  <span class="flow-cell {pos|neg}" title="{date}: {value:.1f}亿"></span>
  ...
</div>
```
CSS：`.flow-cell { display:inline-block; width:8px; height:20px; margin:0 1px; border-radius:2px; }`
`.flow-cell.pos { background:#00C47A; }` `.flow-cell.neg { background:#E84040; }`

### "进入" CTA 按钮（chip 底部）
```python
if st.button("进入 →", key=f"enter_{code}", use_container_width=True):
    st.session_state["tab3_code"] = code
    st.rerun()
```

ScarcityMatrix 渲染架构：整体保持 `st.columns(4)` Tier 分组，每个 chip 用
`st.container()` + `st.html()` 渲染卡片 HTML，`st.button` 紧跟其后。

### 设计 Token
```css
.tier-badge { padding: 2px 6px; border-radius: 4px; font-size: 11px; font-weight: 600; }
.rationale  { font-size: 11px; color: #9AA0AC; margin-left: 6px; }
.flow-strip { margin-top: 6px; }
```

## Data Flow

```
load_macro_state()               → macro_state (MacroCyclePanel, Gauge targets)
load_cloud_capex()               → capex_df (MacroCyclePanel table)
load_scarcity_matrix()           → scarcity_df (ScarcityMatrix)
load_latest_holdings()           → holdings_df (current ratios for Gauge, holding_codes)
load_rebalance_history()         → history_df (HistoryTimeline)
compute_trigger_status(...)      → triggers_df (RebalanceTriggers table)
load_fund_flow_5d(codes, days=20)→ fund_flow_df (双图左图 + mini strip)
load_market_breadth(days=20)     → breadth_df (双图右图)
```

## Test Cases

| ID | 场景 | 步骤 | 期望结果 |
|---|---|---|---|
| TC-019-01 | green 目标 | macro_state="green" | 弹性仪表盘目标显示 38% |
| TC-019-02 | red 目标 | macro_state="red" | 弹性仪表盘目标显示 20% |
| TC-019-03 | 触发颜色（±5%） | deviation=6% | 仪表盘显示 neutral 黄色 |
| TC-019-04 | 触发颜色（±10%） | deviation=11% | 仪表盘显示 bear 红色，触发器行红色背景 |
| TC-019-05 | chip 颜色 | score=72 | 绿色 chip (#00C47A) |
| TC-019-06 | chip 颜色 | score=48 | 红色 chip (#E84040) |
| TC-019-07 | 持有标记 | owned=True 股票 | 显示 ◉ 前缀 |
| TC-019-08 | 手动覆盖 | 点击覆盖 → 选 "red" → 点应用 | set_macro_state_override(MacroState.RED) 调用，data/cache/macro_state.json 写入 {"state":"red"}（实现不写 override 字段） |
| TC-019-09 | mini strip 颜色 | main_net_inflow=[+1,-2,+3,-1,+5] | 格子颜色依次=绿红绿红绿 |
| TC-019-10 | mini strip 空数据 | fund_flow 表无该 code 数据 | 显示 5 个灰色格子，不抛异常 |
| TC-019-11 | CTA 按钮跳转 | 点击 "进入 →" | session_state["tab3_code"] 被设为该 code |
| TC-019-12 | 双图左图 | fund_flow 有 20 天数据 | go.Bar 图不抛异常，x 轴为日期序列 |
| TC-019-13 | 双图右图 | breadth 有 20 天数据 | go.Scatter 图不抛异常，y 轴值在 [0,1] |

## Dependencies

- REQ-001（MacroState enum, set_macro_state_override()）
- REQ-013（inject_global_css，交通灯 glow 样式）
- REQ-020（load_macro_state, load_cloud_capex, load_scarcity_matrix, compute_trigger_status）

## Implementation Notes

- 设计稿 Gauge 使用纯 SVG（polarH/arcH 函数）；实现用 `streamlit.components.v1.html` 嵌入等效 SVG
- 设计稿 HistoryTimeline 使用 SVG foreignObject（Safari 不稳定）；实现用 st.dataframe + 可选 Plotly scatter
- CapEx 列名：fy2025 / fy2026_est（不是设计稿中的 q4_25 / q1_26）
- chipColor 阈值：≥70=bull, ≥50=neutral, <50=bear（与设计稿一致）
- Gauge 双阈值明确区分：±5% 颜色警告，±10% 触发器触发

## Review Notes (evaluator-001 round 1)

Decision: request changes (T04).

- 宏观状态大小写与既有 REQ-001/实现不一致：`engine.macro_gate.MacroState` 和 `data/cache/macro_state.json` 使用 `green/yellow/red` 小写值，但本 REQ 的 AC、示例和 `ELASTIC_TARGETS` 使用 `GREEN/YELLOW/RED`。请统一或明确 normalize 规则，否则 Gauge 会在真实 `macro_state` 上 KeyError。
- `Data Flow` 引用了 `load_cloud_capex()`、`load_scarcity_matrix()`、`load_rebalance_history()`、`compute_trigger_status(...)`，但 REQ-020 没有这些函数的正式定义、schema、空数据行为和缓存策略。请补齐数据接线契约，或把本 REQ 的依赖改到其他明确 REQ。
- 手动覆盖要求调用 `set_macro_state_override()`，但没有说明 UI 传入的是 `MacroState` enum 还是字符串，以及覆盖后如何清理 `st.cache_data`。请补齐调用契约，确保 TC-019-08 可测。
- 请补齐标准 frontmatter 字段（至少 `depends_on`、`test_case_ref`、单句 `acceptance`、`pending_bugs`、`blocked_*`、`pr_number`），并让 `acceptance` 满足 harness §8 的单句可验证规则。

## Review Notes (evaluator-001 round 2)

Decision: request changes (T04).

- Frontmatter acceptance 与正文冲突：frontmatter 写 `state=green → 弹性仪表盘目标显示 33%`，但正文 AC 和 Gauge 合约均写 `green → 38%`。请统一为 38%，否则 TC-019-01 会和 REQ 元数据互相打架。
- 手动覆盖文件格式与 REQ-001 现实现不一致：`engine.macro_gate.set_macro_state_override()` 当前写入 `{"state": "<value>"}`，不会写 `override: true`；但 TC-019-08 期望 `{"state":"red","override":true}`。请要么修改/扩展 REQ-001 契约，要么把本 REQ 的期望改成只断言 state 写入。
- 本 REQ 依赖 REQ-020 的 `load_market_breadth()`，但当前 REQ-020 SQL 示例不可执行；需等 REQ-020 修正后再通过本 REQ。

## TC Impl Review Notes (evaluator-001 round 1)

Decision: request changes (T11).

- `uv run pytest tests/test_app -q` 中所有 REQ-019 app component 测试均因 `ModuleNotFoundError: No module named 'app.components'` 失败。请确保测试 import 目标存在，或将测试改到当前仓库已有模块路径。
- TC-019-03/04 文本要求验证 ±5% 与 ±10% 的区别以及触发器行状态；当前测试只覆盖 `gauge_color()`，没有验证 `compute_trigger_status()` 或 triggered 行红色背景。请补齐触发器状态和样式断言。

## FSM State

draft → req_review → tc_design → tc_review → tc_impl → tc_impl_review → req_impl → req_impl_review → pr_draft → done
