---
req_id: REQ-028
title: "Phase 9 Agent — LangGraph StateGraph + 4 Analysis Nodes + Runner"
status: tc_review
owner: optimizer-001
priority: P0
phase: PHASE-009
scope: engine/agent/graph.py, engine/agent/nodes.py, engine/agent/runner.py
tc_policy: required
tc_exempt_reason: ""
depends_on: [REQ-027, REQ-024, REQ-026]
test_case_ref: [TC-028-01, TC-028-02, TC-028-03, TC-028-04, TC-028-05]
acceptance: "graph.compile() 不抛异常；全 mock 注入下 run_analysis(snapshot) 返回含 signals/reasoning/kg_subgraph/rag_chunks 的 AnalysisState；portfolio_analyzer 写入 context 字符串；kg_retrieval 跳过 error 结果"
review_round: 0
pending_bugs: []
blocked_reason: ""
blocked_from_status: ""
blocked_from_owner: ""
pr_number: ""
---

# Goal

搭建 Phase 9 LangGraph `StateGraph`，将 4 个分析节点串联成：

```
portfolio_analyzer → kg_retrieval → rag_retrieval → signal_ranker → END
```

所有外部依赖（KGQuerier、retrieve、LLM）均可注入，支持无 Neo4j/LanceDB 连接的单元测试。

# In Scope

## `engine/agent/nodes.py`

```python
from __future__ import annotations

import json
from typing import Any, Callable

from langchain_anthropic import ChatAnthropic
from langchain_core.messages import HumanMessage, SystemMessage

from engine.agent.state import AnalysisState, FrozenMarketSnapshot
from engine.kg.query import KGQuerier
from engine.schemas import StockSignal


def portfolio_analyzer(state: AnalysisState) -> dict[str, Any]:
    """Extract holding context strings from snapshot. No external calls."""
    snap: FrozenMarketSnapshot = state["snapshot"]
    context: dict[str, list[str]] = {}
    for h in snap.holdings:
        ctx = [
            f"持仓成本: {h.cost_price:.2f}",
            f"当前价格: {h.current_price:.2f}",
            f"市值: {h.market_value:.0f}",
            f"板块: {h.sector}",
        ]
        context[h.code] = ctx
    return {"kg_subgraph": context, "rag_chunks": {}, "errors": []}


def kg_retrieval(
    state: AnalysisState,
    *,
    kg_querier: KGQuerier | None = None,
) -> dict[str, Any]:
    """Query KG for each holding. Skips on error (degraded mode)."""
    snap = state["snapshot"]
    kg_subgraph: dict[str, list[str]] = dict(state.get("kg_subgraph", {}))
    errors: list[str] = list(state.get("errors", []))

    if kg_querier is None:
        return {"kg_subgraph": kg_subgraph, "errors": errors}

    for h in snap.holdings:
        question = f"{h.name}（{h.code}）属于哪些板块和主题概念？有哪些机构持仓？"
        result = kg_querier.query(question)
        if result.error:
            errors.append(f"KG[{h.code}]: {result.error}")
        else:
            existing = kg_subgraph.get(h.code, [])
            kg_subgraph[h.code] = existing + [result.answer]

    return {"kg_subgraph": kg_subgraph, "errors": errors}


def rag_retrieval(
    state: AnalysisState,
    *,
    retriever_fn: Callable[[str, int], list[str]] | None = None,
) -> dict[str, Any]:
    """Retrieve RAG chunks for each holding. Skips if no retriever injected."""
    snap = state["snapshot"]
    rag_chunks: dict[str, list[str]] = {}
    errors: list[str] = list(state.get("errors", []))

    if retriever_fn is None:
        return {"rag_chunks": rag_chunks, "errors": errors}

    for h in snap.holdings:
        try:
            chunks = retriever_fn(h.name, 5)
            rag_chunks[h.code] = chunks
        except Exception as exc:
            errors.append(f"RAG[{h.code}]: {exc}")

    return {"rag_chunks": rag_chunks, "errors": errors}


_SIGNAL_RANKER_SYSTEM = """你是一个专业的A股投资助手。根据持仓快照、KG知识图谱上下文和年报RAG检索结果，
对每只股票生成投资信号。

输出严格的JSON数组，每个元素包含：
- code: 股票代码（6位数字）
- name: 股票名称
- category: 持仓类别（白马股 或 弹性股）
- technical_score: 技术面评分 0-100
- fundamental_score: 基本面评分 0-100
- sentiment_score: 情绪面评分 0-100
- composite_score: 综合评分 0-100
- action: 操作建议（中文）
- action_code: 操作代码（strong_add/hold_add/hold/reduce/stop_loss/take_profit_alert）
- reasoning: AI判断理由（1-3句话）
- signals: {} （空对象）

只输出JSON数组，不要包含任何解释文字。"""


def signal_ranker(
    state: AnalysisState,
    *,
    llm: ChatAnthropic | None = None,
) -> dict[str, Any]:
    """Synthesize StockSignals using LLM. Injectable for unit tests."""
    snap = state["snapshot"]
    errors: list[str] = list(state.get("errors", []))

    if llm is None:
        llm = ChatAnthropic(model="claude-sonnet-4-6", max_tokens=2048)  # type: ignore[call-arg]

    user_content = (
        f"宏观状态: {snap.macro_state}\n"
        f"持仓数量: {len(snap.holdings)}\n\n"
        "持仓KG上下文:\n"
        + json.dumps(state.get("kg_subgraph", {}), ensure_ascii=False)
        + "\n\n持仓RAG证据:\n"
        + json.dumps(state.get("rag_chunks", {}), ensure_ascii=False)
        + "\n\n持仓快照:\n"
        + json.dumps(
            [
                {
                    "code": h.code,
                    "name": h.name,
                    "category": h.category,
                    "current_price": h.current_price,
                    "cost_price": h.cost_price,
                }
                for h in snap.holdings
            ],
            ensure_ascii=False,
        )
    )

    try:
        response = llm.invoke(
            [SystemMessage(content=_SIGNAL_RANKER_SYSTEM), HumanMessage(content=user_content)]
        )
        raw = json.loads(str(response.content))
        signals: list[StockSignal] = []
        reasoning: dict[str, str] = {}
        for item in raw:
            r = item.pop("reasoning", "")
            sig = StockSignal(**item, signals={})
            signals.append(sig)
            reasoning[sig.code] = r
        return {"signals": signals, "reasoning": reasoning, "errors": errors}
    except Exception as exc:
        errors.append(f"signal_ranker: {exc}")
        return {"signals": [], "reasoning": {}, "errors": errors}
```

## `engine/agent/graph.py`

```python
from __future__ import annotations

from typing import Any, Callable

from langchain_anthropic import ChatAnthropic
from langgraph.graph import END, StateGraph

from engine.agent.nodes import kg_retrieval, portfolio_analyzer, rag_retrieval, signal_ranker
from engine.agent.state import AnalysisState
from engine.kg.query import KGQuerier


def build_graph(
    *,
    kg_querier: KGQuerier | None = None,
    retriever_fn: Callable[[str, int], list[str]] | None = None,
    llm: ChatAnthropic | None = None,
) -> Any:
    """Build and compile the LangGraph StateGraph. Returns compiled graph."""
    builder = StateGraph(AnalysisState)

    builder.add_node("portfolio_analyzer", portfolio_analyzer)
    builder.add_node(
        "kg_retrieval",
        lambda s: kg_retrieval(s, kg_querier=kg_querier),
    )
    builder.add_node(
        "rag_retrieval",
        lambda s: rag_retrieval(s, retriever_fn=retriever_fn),
    )
    builder.add_node(
        "signal_ranker",
        lambda s: signal_ranker(s, llm=llm),
    )

    builder.set_entry_point("portfolio_analyzer")
    builder.add_edge("portfolio_analyzer", "kg_retrieval")
    builder.add_edge("kg_retrieval", "rag_retrieval")
    builder.add_edge("rag_retrieval", "signal_ranker")
    builder.add_edge("signal_ranker", END)

    return builder.compile()
```

## `engine/agent/runner.py`

```python
from __future__ import annotations

from typing import Any, Callable

from langchain_anthropic import ChatAnthropic

from engine.agent.graph import build_graph
from engine.agent.state import AnalysisState, FrozenMarketSnapshot
from engine.kg.query import KGQuerier


def run_analysis(
    snapshot: FrozenMarketSnapshot,
    *,
    kg_querier: KGQuerier | None = None,
    retriever_fn: Callable[[str, int], list[str]] | None = None,
    llm: ChatAnthropic | None = None,
) -> AnalysisState:
    """Run the full analysis pipeline. All deps injectable for unit tests."""
    graph = build_graph(kg_querier=kg_querier, retriever_fn=retriever_fn, llm=llm)
    initial: AnalysisState = {
        "snapshot": snapshot,
        "kg_subgraph": {},
        "rag_chunks": {},
        "signals": [],
        "reasoning": {},
        "errors": [],
        "session_id": snapshot.session_id,
    }
    result: AnalysisState = graph.invoke(initial)
    return result
```

## `pyproject.toml` 新增依赖

```toml
[dependency-groups]
agent = [
    "langgraph>=0.2",
    "langchain-anthropic>=0.3",
    "langchain-core>=0.3",
]
```

# Out of Scope

- Anti-hallucination 验证（REQ-029）
- Session 持久化（REQ-030）
- Streamlit UI（REQ-031）
- ReAct tool-calling 模式（当前用线性 StateGraph）

# Acceptance Criteria

- `TC-028-01`：`build_graph().compile()` 不抛异常（graph 结构合法）
- `TC-028-02`：`portfolio_analyzer(state)` 对含 2 个 `HoldingRow` 的 snapshot 返回
  `kg_subgraph` dict 含 2 个 key，每个 key 值为非空 list
- `TC-028-03`：`kg_retrieval(state, kg_querier=mock_querier)` 其中 mock_querier.query()
  返回 `KGQueryResult(error=None, answer="光通信")` → `state["kg_subgraph"][code]` 包含 `"光通信"`
- `TC-028-04`：`kg_retrieval(state, kg_querier=mock_querier)` 其中 mock_querier.query()
  返回 `KGQueryResult(error="timeout")` → `state["errors"]` 非空，`kg_subgraph` 不更新
- `TC-028-05`：`run_analysis(snapshot, kg_querier=mock, retriever_fn=mock, llm=mock_llm)`
  返回包含 `signals`/`reasoning`/`kg_subgraph`/`rag_chunks`/`errors` 的 `AnalysisState`

# Implementation Notes

- `kg_querier=None` 时 `kg_retrieval` 直接返回（不调用 KG），允许无 Neo4j 环境运行
- `retriever_fn=None` 时 `rag_retrieval` 同理跳过
- `signal_ranker` 中 LLM 返回非 JSON 时捕获异常，写入 `errors`，返回空 signals（degraded mode）
- `_SIGNAL_RANKER_SYSTEM` 常量被 REQ-029 的 `prompt_contracts.py` 引用并计算 hash
- `langgraph` 使用 `StateGraph(TypedDict)` 模式，节点返回 partial dict，LangGraph 自动 merge
