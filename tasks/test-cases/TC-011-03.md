---
tc_id: TC-011-03
req_id: REQ-011
title: "收益率数据超过 3 个交易日时 STALE 传播到 composite_state"
status: draft
priority: P1
---

## Objective

验证任一支柱 stale 时综合状态短路为 stale。

## Preconditions

- `get_latest_indicator("DGS10")` 和 `DGS2` 返回 4 个交易日前的数据
- CapEx 与风险偏好均为非 stale

## Steps

1. 调用 `MacroRegime.compute(as_of=date(2026, 5, 15))`。

## Expected Result

- `yield_curve_state == "stale"`
- `composite_state == "stale"`

